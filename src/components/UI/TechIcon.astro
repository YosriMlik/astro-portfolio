---
export interface Props {
  name: string;
  src: string;
}

const { name, src } = Astro.props;
---

<img 
  src={src} 
  alt={name} 
  title={name} 
  class="w-8 h-8 md:w-10 md:h-10 object-contain transition-transform hover:scale-110"
  loading="lazy"
/>